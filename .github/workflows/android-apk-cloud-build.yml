name: TaskMaster Pro Max - Cloud APK Build

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test GitHub Actions
        run: |
          echo "GitHub Actions is working"
          echo "TaskMaster Pro Max"
